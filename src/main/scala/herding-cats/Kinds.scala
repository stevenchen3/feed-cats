package example.cats.fundamentals.herding.cats

import cats._
import cats.data._
import cats.implicits._

/**
  * `Learn You a Haskell For Great Good`
  *
  * Types are little labels that values carry so that we can reason about the values. But types have
  * their own little labels, called kinds. A kind is more or less the type of a type.
  *
  */
